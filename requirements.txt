# LatentForge's Python worker dependencies (CPU-only, no CUDA — the local
# recurrent substrates never use a GPU; see docs/DEVELOPMENT.md).
#
# Install with the PyTorch CPU wheel index, e.g.:
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cpu
#
# Pinned to the version verified against this repository's fixed-seed
# outputs (see docs/phase-7-baseline-evidence.md). Requires Python >=3.11,<3.14.
torch==2.13.0
